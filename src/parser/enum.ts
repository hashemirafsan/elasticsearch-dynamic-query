/* A TypeScript enum. */
export enum DataTypeEnum {
  ID = 'ID',
  TEXT = 'TEXT',
  NUMBER = 'NUMBER',
  ARRAY = 'ARRAY',
  DATETIME = 'DATETIME',
  BOOLEAN = 'BOOLEAN'
}

/* A TypeScript enum. */
export enum ElasticTokenEnum {
  TERM = 'term',
  TERMS = 'terms',
  MATCH = 'match',
  RANGE = 'range',
  EXISTS = 'exists',
  REGEXP = 'regexp',
}
