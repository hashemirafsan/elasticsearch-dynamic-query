/* A way to define a type. */
export enum CompoundQueryType {
  BOOL = 'bool',
}

/* A way to define a type. */
export enum ElasticOccurEnum {
  MUST = 'must',
  MUST_NOT = 'must_not',
  SHOULD = 'should',
  FILTER = 'filter',
}
